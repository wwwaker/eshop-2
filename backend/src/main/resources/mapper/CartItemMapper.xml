<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.eshop.dao.CartItemDao">
    <resultMap id="BaseResultMap" type="com.example.eshop.entity.CartItem">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM cart_items WHERE user_id = #{userId}
    </select>
    <select id="findByUserIdAndProductId" resultMap="BaseResultMap">
        SELECT * FROM cart_items WHERE user_id = #{userId} AND product_id = #{productId}
    </select>
    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM cart_items WHERE id = #{id}
    </select>
    <insert id="insert" parameterType="com.example.eshop.entity.CartItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart_items (user_id, product_id, quantity, created_at, updated_at)
        VALUES (#{userId}, #{productId}, #{quantity}, #{createdAt}, #{updatedAt})
    </insert>
    <update id="update" parameterType="com.example.eshop.entity.CartItem">
        UPDATE cart_items SET
            quantity = #{quantity},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE FROM cart_items WHERE id = #{id}
    </delete>
    <delete id="deleteByUserId">
        DELETE FROM cart_items WHERE user_id = #{userId}
    </delete>
</mapper>
