<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.eshop.dao.ProductDao">
    <resultMap id="BaseResultMap" type="com.example.eshop.entity.Product">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="price" column="price"/>
        <result property="stock" column="stock"/>
        <result property="categoryId" column="category_id"/>
        <result property="imageUrl" column="image_url"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="categoryName" column="category_name"/>
    </resultMap>

    <select id="findAllOnSale" resultMap="BaseResultMap">
        SELECT p.*, c.name as category_name
        FROM products p
        LEFT JOIN categories c ON p.category_id = c.id
        WHERE p.status = 'ON_SALE'
        ORDER BY p.id DESC
    </select>
    <select id="findByCategoryId" resultMap="BaseResultMap">
        SELECT p.*, c.name as category_name
        FROM products p
        LEFT JOIN categories c ON p.category_id = c.id
        WHERE p.category_id = #{categoryId} AND p.status = 'ON_SALE'
    </select>
    <select id="searchByName" resultMap="BaseResultMap">
        SELECT p.*, c.name as category_name
        FROM products p
        LEFT JOIN categories c ON p.category_id = c.id
        WHERE p.name LIKE CONCAT('%', #{keyword}, '%') AND p.status = 'ON_SALE'
    </select>
    <select id="searchSuggestions" resultType="java.lang.String">
        SELECT DISTINCT p.name
        FROM products p
        WHERE p.name LIKE CONCAT('%', #{keyword}, '%') AND p.status = 'ON_SALE'
        ORDER BY p.name
            LIMIT 10
    </select>
    <select id="findById" resultMap="BaseResultMap">
        SELECT p.*, c.name as category_name
        FROM products p
        LEFT JOIN categories c ON p.category_id = c.id
        WHERE p.id = #{id}
    </select>

    <!-- 管理员系统方法 -->
    <select id="findAll" resultMap="BaseResultMap">
        SELECT p.*, c.name as category_name
        FROM products p
        LEFT JOIN categories c ON p.category_id = c.id
        ORDER BY p.id DESC
    </select>
    <insert id="insert" parameterType="com.example.eshop.entity.Product"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO products (name, description, price, stock, category_id, image_url, status)
        VALUES (#{name}, #{description}, #{price}, #{stock}, #{categoryId}, #{imageUrl}, #{status})
    </insert>
    <update id="update" parameterType="com.example.eshop.entity.Product">
        UPDATE products
        SET name = #{name},
            description = #{description},
            price = #{price},
            stock = #{stock},
            category_id = #{categoryId},
            image_url = #{imageUrl},
            status = #{status}
        WHERE id = #{id}
    </update>
    <update id="updateStock">
        UPDATE products SET stock = #{stock} WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM products WHERE id = #{id}
    </delete>
    <select id="countTodayNewProducts" resultType="int">
        SELECT COUNT(*)
        FROM products
        WHERE DATE(created_at) = CURDATE()
    </select>
    <select id="getSalesTrend" resultType="java.util.Map">
        SELECT
            DATE(o.created_at) as date,
            COUNT(o.id) as orderCount,
            COALESCE(SUM(o.total_amount), 0) as totalAmount
        FROM orders o
        WHERE o.created_at &gt;= DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
        GROUP BY DATE(o.created_at)
        ORDER BY DATE(o.created_at)
    </select>
    <select id="getHotProducts" resultType="java.util.Map">
        SELECT
            p.id,
            p.name,
            p.price,
            p.image_url as imageUrl,
            COALESCE(SUM(oi.quantity), 0) as totalSold
        FROM products p
        LEFT JOIN order_items oi ON p.id = oi.product_id
        GROUP BY p.id, p.name, p.price, p.image_url
        ORDER BY totalSold DESC
        LIMIT #{limit}
    </select>
    <update id="moveToCategory">
        UPDATE products
        SET category_id = #{toCategoryId}
        WHERE category_id = #{fromCategoryId}
    </update>
</mapper>
