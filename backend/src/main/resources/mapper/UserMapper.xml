<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.eshop.dao.UserDao">
    <resultMap id="BaseResultMap" type="com.example.eshop.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="address" column="address"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>
    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT * FROM users WHERE email = #{email}
    </select>
    <insert id="insert" parameterType="com.example.eshop.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, phone, address, role, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{address}, #{role}, #{createdAt}, #{updatedAt})
    </insert>
    <update id="update" parameterType="com.example.eshop.entity.User">
        UPDATE users SET
            email = #{email},
            phone = #{phone},
            address = #{address},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <!-- 管理员系统方法 -->
    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM users
    </select>
    <select id="findAllWithFilters" resultMap="BaseResultMap" parameterType="map">
        SELECT * FROM users
        WHERE 1=1
        <if test="search != null and search != ''">
            AND (username LIKE CONCAT('%', #{search}, '%') OR email LIKE CONCAT('%', #{search}, '%') OR phone LIKE CONCAT('%', #{search}, '%'))
        </if>
        <if test="role != null and role != 'all'">
            AND role = #{role}
        </if>
        <if test="sortField != null and sortField != ''">
            ORDER BY ${sortField}
            <if test="sortOrder != null and sortOrder != ''">
                ${sortOrder}
            </if>
        </if>
        <if test="sortField == null or sortField == ''">
            ORDER BY id DESC
        </if>
    </select>
    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>
    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>
    <select id="countTodayNewUsers" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE DATE(created_at) = CURDATE()
    </select>

    <select id="findAllWithPagination" resultMap="BaseResultMap" parameterType="map">
        SELECT * FROM users
        WHERE 1=1
        <if test="search != null and search != ''">
            AND (username LIKE CONCAT('%', #{search}, '%') OR email LIKE CONCAT('%', #{search}, '%') OR phone LIKE CONCAT('%', #{search}, '%'))
        </if>
        <if test="role != null and role != 'all'">
            AND role = #{role}
        </if>
        <if test="sortField != null and sortField != ''">
            ORDER BY ${sortField}
            <if test="sortOrder != null and sortOrder != ''">
                ${sortOrder}
            </if>
        </if>
        <if test="sortField == null or sortField == ''">
            ORDER BY id DESC
        </if>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countWithFilters" resultType="int" parameterType="map">
        SELECT COUNT(*)
        FROM users
        WHERE 1=1
        <if test="search != null and search != ''">
            AND (username LIKE CONCAT('%', #{search}, '%') OR email LIKE CONCAT('%', #{search}, '%') OR phone LIKE CONCAT('%', #{search}, '%'))
        </if>
        <if test="role != null and role != 'all'">
            AND role = #{role}
        </if>
    </select>
</mapper>
